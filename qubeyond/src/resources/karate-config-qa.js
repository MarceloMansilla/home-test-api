/**
 * QA overrides - merged on top of karate-config.js.
 *
 * URL, timeouts and flags live in config/environments/config-qa.yml.
 * Add entries here only when an override needs logic (secrets, tokens,
 * conditional setup). Returning an empty object is a no-op.
 */
function fn() {
  return {};
}
