# Development environment
url:
  protocol: https
  host: api.develop.com
  # port omitted -> the protocol default (443) is used

config:
  debug: true
  ssl: false
  connectTimeout: 10000
  readTimeout: 10000
  mockExternalServices: true

# Seed data the scenarios depend on - placeholders, like the host above.
# Align them with the real dev seed catalogue before using this environment.
testData:
  filterId: "3"
  existingItemId: "1"
  minItemCount: 9
