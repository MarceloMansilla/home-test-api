# Staging environment
url:
  protocol: https
  host: api.staging.com
  # port omitted -> the protocol default (443) is used

config:
  debug: true
  ssl: false
  connectTimeout: 15000
  readTimeout: 15000
  mockExternalServices: false

# Seed data the scenarios depend on - placeholders, like the host above.
# Align them with the real staging seed catalogue before using this environment.
testData:
  filterId: "3"
  existingItemId: "1"
  minItemCount: 9
